import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#1a73e8',
    },
    secondary: {
      main: '#5f6368',
    },
    success: {
      main: '#34A853',
    },
  },
});

export default theme;
